package mart30;

public class m15_Math {
    /*
                The Java Math class has many methods that allows you to perform mathematical tasks on numbers.

                Math.max(x,y)
              The Math.max(x,y) method can be used to find the highest value of x and y:

                Example
                Math.max(5, 10);

            Math.min(x,y)
            The Math.min(x,y) method can be used to find the lowest value of x and y:

                Example
                Math.min(5, 10);

            Math.sqrt(x)
            The Math.sqrt(x) method returns the square root of x:

                Example
                Math.sqrt(64);

            Math.abs(x)
            The Math.abs(x) method returns the absolute (positive) value of x:

                Example
                Math.abs(-4.7);

            Random Numbers
            Math.random() returns a random number between 0.0 (inclusive), and 1.0 (exclusive):

                Example
                Math.random();

            To get more control over the random number, e.g. you only want a random number between
            0 and 100, you can use the following formula:

                Example
                int randomNum = (int)(Math.random() * 101);  // 0 to 100

            Complete Math Reference
            For a complete reference of Math methods, go to our Java Math Methods Reference.







            ------Java Math sınıfında, sayılar üzerinde matematiksel görevler gerçekleştirmenize
            izin veren birçok yöntem bulunur.

            Math.max(x,y)
            Math.max(x,y) yöntemi, x ve y'nin en yüksek değerini bulmak için kullanılabilir:

            Örnek vermek
            Math.max(5, 10);

            Math.min(x,y)
            Math.min(x,y) yöntemi, x ve y'nin en düşük değerini bulmak için kullanılabilir:

            Örnek
            Math.min(5, 10);

            Math.sqrt(x)
            Math.sqrt(x) yöntemi, x'in karekökünü döndürür:

            Örnek
            Math.sqrt(64);

            Math.abs(x)
            Math.abs(x) yöntemi, x'in mutlak (pozitif) değerini döndürür:

            Örnek
            Math.abs(-4.7);

            Rastgele numaralar
            Math.random(), 0.0 (dahil) ve 1.0 (hariç) arasında rastgele bir sayı döndürür:

            Örnek
            Math.rastgele();

            Rastgele sayı üzerinde daha fazla kontrol elde etmek için,
             ör. sadece 0 ile 100 arasında rastgele bir sayı istiyorsanız, aşağıdaki formülü kullanabilirsiniz:

            Örnek
            int randomNum = (int)(Math.random() * 101); // 0'dan 100'e

            Matematik Referansını Tamamla
            Matematik yöntemlerinin eksiksiz bir referansı için Java Matematik Yöntemleri Referansımıza gidin.
     */
}
