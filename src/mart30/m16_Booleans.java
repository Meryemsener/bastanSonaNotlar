package mart30;

public class m16_Booleans {
    /*
            ------Java Booleans
            Very often, in programming, you will need a data type that can only have one of two values, like:

            YES / NO
            ON / OFF
            TRUE / FALSE
            For this, Java has a boolean data type, which can take the values true or false.

        ----Boolean Values
        A boolean type is declared with the boolean keyword and can only take the values true or false:

            Example
            boolean isJavaFun = true;
            boolean isFishTasty = false;
            System.out.println(isJavaFun);     // Outputs true
            System.out.println(isFishTasty);   // Outputs false

        However, it is more common to return boolean values from boolean
         expressions, for conditional testing (see below).

        ----Boolean Expression
        A Boolean expression is a Java expression that returns a Boolean value: true or false.

        You can use a comparison operator, such as the greater than (>) operator
         to find out if an expression (or a variable) is true:

        Example
        int x = 10;
        int y = 9;
        System.out.println(x > y); // returns true, because 10 is higher than 9

        Or even easier:

        Example
        System.out.println(10 > 9); // returns true, because 10 is higher than 9

        In the examples below, we use the equal to (==) operator to evaluate an
         expression:

        Example
        int x = 10;
        System.out.println(x == 10); // returns true, because the value of x is equal to 10

        Example
        System.out.println(10 == 15); // returns false, because 10 is not equal to 15

        The Boolean value of an expression is the basis for all Java comparisons and conditions.

        You will learn more about conditions in the next chapter.





        -----Java Booleanları
        Çok sık olarak, programlamada, aşağıdaki gibi iki değerden yalnızca birine
        sahip olabilen bir veri türüne ihtiyacınız olacaktır:

            EVET HAYIR
            AÇIK KAPALI
            DOĞRU YANLIŞ
            Bunun için Java, true veya false değerlerini alabilen bir boolean veri türüne
            sahiptir.

        ----Boole Değerleri
        Boolean anahtar sözcüğüyle bir boole türü bildirilir ve yalnızca true veya false değerlerini alabilir:

        Örnek
        boolean isJavaFun = true;
        boolean isFishTasty = yanlış;
        System.out.println(isJavaFun); // çıktılar doğru
        System.out.println(isFishTasty); // yanlış çıktı verir

        Ancak, koşullu test için boole ifadelerinden boole değerleri
        döndürmek daha yaygındır (aşağıya bakın).

        ----Boolean ifadesi
       Boole ifadesi, bir Boole değeri döndüren bir Java ifadesidir: true veya false.

     Bir ifadenin (veya bir değişkenin) doğru olup olmadığını öğrenmek için büyüktür (>) operatörü gibi bir karşılaştırma operatörü kullanabilirsiniz:

        Örnek vermek
        int x = 10;
        int y = 9;
        System.out.println(x > y); // 10, 9'dan büyük olduğu için true değerini döndürür

        Veya daha da kolay:

        Örnek vermek
        System.out.println(10 > 9); // 10, 9'dan büyük olduğu için true değerini döndürür

        Aşağıdaki örneklerde, bir ifadeyi değerlendirmek için eşittir (==) operatörünü kullanıyoruz:

        Örnek vermek
        int x = 10;
        System.out.println(x == 10); // x'in değeri 10'a eşit olduğu için true değerini döndürür

        Örnek vermek
        System.out.println(10 == 15); // false döndürür, çünkü 10, 15'e eşit değildir

        Bir ifadenin Boole değeri, tüm Java karşılaştırmaları ve koşullarının temelidir.

        Bir sonraki bölümde koşullar hakkında daha fazla bilgi edineceksiniz.



     */
}
