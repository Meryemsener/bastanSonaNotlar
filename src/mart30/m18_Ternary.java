        package mart30;

        public class m18_Ternary {
            /*
        Java Short Hand If...Else (Ternary Operator)
        Short Hand If...Else
        There is also a short-hand if else, which is known as the ternary operator because it consists of three operands.

        It can be used to replace multiple lines of code with a single line, and is most often used to replace simple if else statements:

        Syntax
        variable = (condition) ? expressionTrue :  expressionFalse;
        Instead of writing:

        Example
        int time = 20;
        if (time < 18) {
          System.out.println("Good day.");
        } else {
          System.out.println("Good evening.");
        }

        You can simply write:

        Example
        int time = 20;
        String result = (time < 18) ? "Good day." : "Good evening.";
        System.out.println(result);



        Kısa El If...Else
        Ayrıca, üç işlenenden oluştuğu için üçlü operatör olarak bilinen başka bir kısa el de vardır.

        Birden çok kod satırını tek bir satırla değiştirmek için kullanılabilir ve çoğunlukla basit if deyimlerini değiştirmek için kullanılır:

        Sözdizimi
        değişken = (koşul) ? ifadeTrue : ifadeFalse;
        Yazmak yerine:

        Örnek
        int zaman = 20;
        if (zaman < 18) {
          System.out.println("İyi günler.");
        } else {
          System.out.println("İyi akşamlar.");
        }

        Basitçe yazabilirsiniz:

        Örnek vermek
        int zaman = 20;
        Dizi sonucu = (zaman < 18) ? "İyi günler." : (zaman<20) ? "İyi geceler.": "yanlis giris";
        System.out.println(sonuç);




             */
        }
