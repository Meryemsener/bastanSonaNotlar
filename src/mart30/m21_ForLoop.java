        package mart30;

        public class m21_ForLoop {
            /*
        Java For Loop
        When you know exactly how many times you want to loop through a block of code, use the for loop instead of a while loop:

        Syntax
        for (statement 1; statement 2; statement 3) {
          // code block to be executed
        }
        Statement 1 is executed (one time) before the execution of the code block.

        Statement 2 defines the condition for executing the code block.

        Statement 3 is executed (every time) after the code block has been executed.

        The example below will print the numbers 0 to 4:

        Example
        for (int i = 0; i < 5; i++) {
          System.out.println(i);
        }

        Example explained
        Statement 1 sets a variable before the loop starts (int i = 0).

        Statement 2 defines the condition for the loop to run (i must be less than 5). If the condition is true, the loop will start over again, if it is false, the loop will end.

        Statement 3 increases a value (i++) each time the code block in the loop has been executed.

        Another Example
        This example will only print even values between 0 and 10:

        Example
        for (int i = 0; i <= 10; i = i + 2) {
          System.out.println(i);
        }

        For-Each Loop
        There is also a "for-each" loop, which is used exclusively to loop through elements in an array:

        Syntax
        for (type variableName : arrayName) {
          // code block to be executed
        }
        The following example outputs all elements in the cars array, using a "for-each" loop:

        Example
        String[] cars = {"Volvo", "BMW", "Ford", "Mazda"};
        for (String i : cars) {
          System.out.println(i);
        }

        Note: Don't worry if you don't understand the example above. You will learn more about Arrays in the Java Arrays chapter.








        Döngü için Java
        Bir kod bloğunda tam olarak kaç kez döngü yapmak istediğinizi biliyorsanız, while döngüsü yerine for döngüsünü kullanın:

        Sözdizimi
        for (ifade 1; ifade 2; ifade 3) {
          // yürütülecek kod bloğu
        }
        İfade 1, kod bloğunun yürütülmesinden önce (bir kez) yürütülür.

        İfade 2, kod bloğunun yürütülmesi için koşulu tanımlar.

        Kod bloğu yürütüldükten sonra (her seferinde) İfade 3 yürütülür.

        Aşağıdaki örnek, 0'dan 4'e kadar olan sayıları yazdıracaktır:

        Örnek vermek
        for (int i = 0; i < 5; i++) {
          System.out.println(i);
        }

        Örnek açıklama
        İfade 1, döngü başlamadan önce bir değişken ayarlar (int i = 0).

        İfade 2, döngünün çalışması için koşulu tanımlar (i 5'ten küçük olmalıdır). Koşul doğruysa döngü yeniden başlar, yanlışsa döngü sona erer.

        İfade 3, döngüdeki kod bloğu her çalıştırıldığında bir değeri (i++) artırır.

        Başka bir örnek
        Bu örnek yalnızca 0 ile 10 arasındaki çift değerleri yazdıracaktır:

        Örnek vermek
        for (int ben = 0; ben <= 10; ben = ben + 2) {
          System.out.println(i);
        }
        Her Döngü İçin
        Ayrıca, yalnızca bir dizideki öğeler arasında dolaşmak için kullanılan "her biri için" bir döngü vardır:

        Sözdizimi
        for (değişkenAdı yazın: diziAdı) {
          // yürütülecek kod bloğu
        }
        Aşağıdaki örnek, bir "for-each" döngüsü kullanarak arabalar dizisindeki tüm öğelerin çıktısını verir:

        Örnek vermek
        String[] arabalar = {"Volvo", "BMW", "Ford", "Mazda"};
        for (dize i: arabalar) {
          System.out.println(i);
        }

        Not: Yukarıdaki örneği anlamadıysanız endişelenmeyin. Java Dizileri bölümünde Diziler hakkında daha fazla bilgi edineceksiniz.


             */
        }
