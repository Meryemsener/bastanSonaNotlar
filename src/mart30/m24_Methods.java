        package mart30;

        public class m24_Methods {
            /*
            Java Methods
        A method is a block of code which only runs when it is called.

        You can pass data, known as parameters, into a method.

        Methods are used to perform certain actions, and they are also known as functions.

        Why use methods? To reuse code: define the code once, and use it many times.

        Create a Method
        A method must be declared within a class. It is defined with the name of the method, followed by parentheses (). Java provides some pre-defined methods, such as System.out.println(), but you can also create your own methods to perform certain actions:

        Example
        Create a method inside Main:

        public class Main {
          static void myMethod() {
            // code to be executed
          }
        }

        Example Explained
        myMethod() is the name of the method
        static means that the method belongs to the Main class and not an object of the Main class. You will learn more about objects and how to access methods through objects later in this tutorial.
        void means that this method does not have a return value. You will learn more about return values later in this chapter
        Call a Method
        To call a method in Java, write the method's name followed by two parentheses () and a semicolon;

        In the following example, myMethod() is used to print a text (the action), when it is called:

        Example
        Inside main, call the myMethod() method:

        public class Main {
          static void myMethod() {
            System.out.println("I just got executed!");
          }

          public static void main(String[] args) {
            myMethod();
          }
        }

        // Outputs "I just got executed!"


        A method can also be called multiple times:

        Example
        public class Main {
          static void myMethod() {
            System.out.println("I just got executed!");
          }

          public static void main(String[] args) {
            myMethod();
            myMethod();
            myMethod();
          }
        }

        // I just got executed!
        // I just got executed!
        // I just got executed!




        In the next chapter, Method Parameters, you will learn how to pass data (parameters) into a method.





        Yöntem, yalnızca çağrıldığında çalışan bir kod bloğudur.

        Parametre olarak bilinen verileri bir yönteme geçirebilirsiniz.

        Yöntemler, belirli eylemleri gerçekleştirmek için kullanılır ve işlevler olarak da bilinirler.

        Neden yöntemler kullanılır? Kodu yeniden kullanmak için: kodu bir kez tanımlayın ve birçok kez kullanın.

        Yöntem Oluştur
        Bir sınıf içinde bir metot bildirilmelidir. Yöntemin adı ve ardından parantez () ile tanımlanır. Java, System.out.println() gibi bazı önceden tanımlanmış yöntemler sağlar, ancak belirli eylemleri gerçekleştirmek için kendi yöntemlerinizi de oluşturabilirsiniz:

        Örnek vermek
        Main içinde bir yöntem oluşturun:

        genel sınıf Ana {
          statik geçersiz myMethod() {
            // yürütülecek kod
          }
        }

        Örnek Açıklama
        myMethod() yöntemin adıdır
        static, yöntemin Main sınıfının bir nesnesi değil, Main sınıfına ait olduğu anlamına gelir. Bu öğreticide daha sonra nesneler ve nesneler aracılığıyla yöntemlere nasıl erişileceği hakkında daha fazla bilgi edineceksiniz.
        void, bu yöntemin bir dönüş değerine sahip olmadığı anlamına gelir. Bu bölümde daha sonra dönüş değerleri hakkında daha fazla bilgi edineceksiniz.
        Yöntem Çağır
        Java'da bir yöntemi çağırmak için, yöntemin adını ve ardından iki parantez () ve noktalı virgül yazın;

        Aşağıdaki örnekte, çağrıldığında bir metni (eylem) yazdırmak için myMethod() kullanılır:
        Örnek vermek
        Main içinde myMethod() yöntemini çağırın:

        genel sınıf Ana {
          statik geçersiz myMethod() {
            System.out.println("Az önce idam edildim!");
          }

          public static void main(String[] args) {
            benimYöntem();
          }
        }

        // "Az önce idam edildim!" çıktısı verir.


        Bir yöntem birden çok kez de çağrılabilir:

        Örnek vermek
        genel sınıf Ana {
          statik geçersiz myMethod() {
            System.out.println("Az önce idam edildim!");
          }

          public static void main(String[] args) {
            benimYöntem();
            benimYöntem();
            benimYöntem();
          }
        }

        // Az önce idam edildim!
        // Az önce idam edildim!
        // Az önce idam edildim!




        Bir sonraki bölümde, Yöntem Parametreleri'nde, verilerin (parametrelerin) bir yönteme nasıl aktarılacağını öğreneceksiniz.



             */
        }
