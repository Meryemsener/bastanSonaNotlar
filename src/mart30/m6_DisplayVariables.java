package mart30;

public class m6_DisplayVariables {
    /*
       Display Variables
    The println() method is often used to display variables.
    To combine both text and a variable, use the + character:

    Example
    String name = "John";
   System.out.println("Hello " + name);

    You can also use the + character to add a variable to another variable:

   Example
   String firstName = "John ";
   String lastName = "Doe";
   String fullName = firstName + lastName;
   System.out.println(fullName);

   For numeric values, the + character works as a mathematical operator (notice that we use int (integer) variables here):

   Example
   int x = 5;
   int y = 6;
   System.out.println(x + y); // Print the value of x + y

   From the example above, you can expect:

   x stores the value 5
   y stores the value 6
   Then we use the println() method to display the value of x + y, which is 11




   ---Değişkenleri Görüntüle
     println() yöntemi genellikle değişkenleri görüntülemek için kullanılır.

    Hem metni hem de bir değişkeni birleştirmek için + karakterini kullanın:

    Örnek vermek
    Dize adı = "John";
    System.out.println("Merhaba" + isim);
    Başka bir değişkene bir değişken eklemek için + karakterini de kullanabilirsiniz:

    Örnek vermek
    String firstName = "John";
    String lastName = "Doe";
    String fullName = ad + soyad;
    System.out.println(tamAd);

    Sayısal değerler için + karakteri matematiksel bir operatör olarak çalışır (burada int (tamsayı)
    değişkenleri kullandığımıza dikkat edin):

   Örnek vermek
   int x = 5;
   int y = 6;
   System.out.println(x + y); // x + y değerini yazdır

   Yukarıdaki örnekten şunları bekleyebilirsiniz:

   x, 5 değerini saklar
   y 6 değerini saklar
    Daha sonra x + y'nin 11 olan değerini görüntülemek için println() yöntemini kullanırız.

     */
}
