package mart30;

public class m7_DeclareManyVariables {
    /*
    To declare more than one variable of the same type, you can use a comma-separated list:

        Example
        Instead of writing:

            int x = 5;
            int y = 6;
            int z = 50;
        System.out.println(x + y + z);

        You can simply write:

            int x = 5, y = 6, z = 50;
            System.out.println(x + y + z);

        One Value to Multiple Variables
        You can also assign the same value to multiple variables in one line:

        Example
            int x, y, z;
            x = y = z = 50;
            System.out.println(x + y + z);




        Aynı türden birden fazla değişken bildirmek için virgülle ayrılmış bir liste kullanabilirsiniz:

        Örnek vermek
        Yazmak yerine:

            int x = 5;
            int y = 6;
            int z = 50;
           System.out.println(x + y + z);

    Basitçe yazabilirsiniz:

            int x = 5, y = 6, z = 50;
        System.out.println(x + y + z);

        Birden Çok Değişkene Bir Değer
        Aynı değeri bir satırda birden çok değişkene de atayabilirsiniz:

        Örnek vermek
        int x, y, z;
        x = y = z = 50;
    System.out.println(x + y + z);

     */

}
