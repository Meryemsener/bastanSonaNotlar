        package mart30_javaOOP;

        public class k2_ClassesObjects {
            /*
            Java Classes/Objects
        Java is an object-oriented programming language.

        Everything in Java is associated with classes and objects, along with
         its attributes and methods. For example: in real life, a car is an object.
          The car has attributes, such as weight and color, and methods, such as drive
          and brake.

        A Class is like an object constructor, or a "blueprint" for creating objects.

        Create a Class
        To create a class, use the keyword class:

        Main.java
        Create a class named "Main" with a variable x:

        public class Main {
          int x = 5;
        }
        Remember from the Java Syntax chapter that a class should always
        start with an uppercase first letter, and that the name of the java
        file should match the class name.

        Create an Object
        In Java, an object is created from a class. We have already created the
        class named Main, so now we can use this to create objects.

        To create an object of Main, specify the class name, followed by the object
         name, and use the keyword new:

        Example
        Create an object called "myObj" and print the value of x:

        public class Main {
          int x = 5;

          public static void main(String[] args) {
            Main myObj = new Main();
            System.out.println(myObj.x);
          }
        }


        Java Sınıfları/Nesneleri
        Java, nesne yönelimli bir programlama dilidir.

        Java'daki her şey, nitelikleri ve yöntemleri ile birlikte sınıflar ve
         nesnelerle ilişkilidir. Örneğin: gerçek hayatta araba bir nesnedir.
         Otomobilin ağırlık ve renk gibi özellikleri ve sürüş ve fren gibi yöntemleri vardır.

        Bir Sınıf, bir nesne oluşturucu veya nesneler oluşturmak için bir "plan" gibidir.

        Sınıf Oluştur
        Bir sınıf oluşturmak için sınıf anahtar sözcüğünü kullanın:

        ana.java
        x değişkeni ile "Main" adında bir sınıf oluşturun:

        genel sınıf Ana {
          int x = 5;
        }
        Java Sözdizimi bölümünden, bir sınıfın her zaman bir büyük harfle başlaması
        gerektiğini ve Java dosyasının adının sınıf adıyla eşleşmesi gerektiğini unutmayın.

        Bir Nesne Oluştur
        Java'da, bir sınıftan bir nesne oluşturulur. Main adlı sınıfı zaten oluşturduk,
        şimdi bunu nesneler oluşturmak için kullanabiliriz.

        Bir Main nesnesi oluşturmak için, sınıf adını ve ardından nesne adını belirtin
         ve new anahtar sözcüğünü kullanın:

        Örnek vermek
        "myObj" adında bir nesne oluşturun ve x'in değerini yazdırın:

        genel sınıf Ana {
          int x = 5;

          public static void main(String[] args) {
            Ana myObj = yeni Ana();
            System.out.println(myObj.x);
          }
        }



             */
        }
